<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="info.rashadtanjim.interactivephotogallery">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!--    Note: need to use write permission and it is restricted above Android 29, so I need to maxSdkVersion to check OS version
            Reason: The app uses storage to save photo and share photo -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:extractNativeLibs="true"
        android:icon="@drawable/ic_gallery"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_gallery"
        android:theme="@style/Theme.InteractivePhotoGallery">

        <activity
            android:name=".ui.GalleryActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="interactivephotogallery.page.link"
                    android:scheme="https" />

                <data
                    android:host="interactivephotogallery.page.link"
                    android:pathPrefix="/picsum"
                    android:scheme="https" />

                <data
                    android:host="www.interactivephotogallery.page.link"
                    android:pathPrefix="/picsum"
                    android:scheme="https" />
            </intent-filter>

        </activity>

        <activity
            android:name=".ui.MainActivity"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>